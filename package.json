{
  "name": "uk-budget-battle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "check": "tsc --noEmit",
    "types": "wrangler types",
    "build": "node scripts/build.mjs",
    "predev": "npm run build",
    "predeploy": "npm run build",
    "check:browser": "node scripts/browser-check.mjs && node scripts/presets-check.mjs && node scripts/walkthrough-check.mjs",
    "check:live": "node scripts/live-check.mjs && BASE_URL=https://uk-budget-battle.openuk-co.workers.dev node scripts/presets-check.mjs && BASE_URL=https://uk-budget-battle.openuk-co.workers.dev node scripts/walkthrough-check.mjs",
    "build:social": "node scripts/social-card.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "playwright": "1.62.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.0",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "engines": {
    "node": ">=22"
  }
}
